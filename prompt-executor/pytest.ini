[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
    --cov=prompt_executor
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-fail-under=100
